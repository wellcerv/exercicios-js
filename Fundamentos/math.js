const raio = 5.6;
const area = Math.PI * Math.pow(raio, 2); // Math é uma função do JS - Neste caso estamos calculando a áera de uma circunferencia

console.log(area.toFixed(2));
console.log(typeof Math);